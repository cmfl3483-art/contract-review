# 合同预审看板系统 - 测试指南

## 系统状态
✅ 所有服务正常运行
✅ 所有已知问题已修复
✅ API 测试通过

## 测试步骤

### 1. 清除浏览器缓存（重要！）
**Safari 浏览器**:
1. 按 `Command + Option + E` 清空缓存
2. 或者：菜单栏 → 开发 → 清空缓存
3. **完全关闭 Safari 浏览器**
4. 重新打开 Safari

### 2. 访问系统
1. 打开浏览器
2. 访问: `https://underfed-isolating-prolonged.ngrok-free.dev`
3. 应该自动跳转到钉钉登录（如果未登录）
4. 登录后应该看到合同列表页面

### 3. 测试合同列表
**预期结果**:
- ✅ 页面正常加载，没有"组件加载失败"错误
- ✅ 右下角显示您的名字（陈敏）
- ✅ 看到 1 个合同：xxxx2026人月框架合同
- ✅ 可以切换筛选条件（全部/进行中/已完成/待我处理/抄送我）

### 4. 测试合同详情
1. 点击合同列表中的合同
2. **预期结果**:
   - ✅ 页面正常加载，没有错误
   - ✅ 显示合同名称和描述
   - ✅ 显示发起人信息（陈敏）
   - ✅ 显示评审人列表（周九 - 法务）
   - ✅ 显示评审状态（待处理）

### 5. 测试创建合同
1. 点击"发起合同预审"按钮
2. 填写表单：
   - 合同名称: 测试合同
   - 合同描述: 这是一个测试合同
   - 评审人: 选择一个或多个（张三、李四、王五等）
3. 点击"提交"
4. **预期结果**:
   - ✅ 显示"合同创建成功"提示
   - ✅ 对话框关闭
   - ✅ 合同列表自动刷新
   - ✅ 新合同出现在列表中

## 如果仍然出现问题

### 问题：页面显示"组件加载失败"
**解决方案**:
1. 打开浏览器开发者工具（`Command + Option + I`）
2. 切换到"控制台"标签
3. 截图控制台中的错误信息
4. 切换到"网络"标签
5. 刷新页面
6. 找到失败的请求（红色的）
7. 点击查看详细信息
8. 截图请求和响应内容

### 问题：API 返回 500 错误
**解决方案**:
```bash
# 1. 查看后端日志
docker-compose logs backend --tail=100

# 2. 清除 Redis 缓存
docker-compose exec redis redis-cli FLUSHALL

# 3. 重启后端
docker-compose restart backend

# 4. 等待 10 秒后重试
```

### 问题：Authorization 头缺失
**解决方案**:
1. 打开浏览器控制台
2. 运行: `localStorage.getItem('user-storage')`
3. 如果返回 null，需要重新登录
4. 清除 localStorage: `localStorage.clear()`
5. 刷新页面，重新登录

## 验证清单

- [ ] 可以正常登录
- [ ] 右下角显示用户名
- [ ] 合同列表正常显示
- [ ] 可以点击合同查看详情
- [ ] 合同详情页面正常显示
- [ ] 可以创建新合同
- [ ] 评审人列表显示真实用户
- [ ] 创建合同后列表自动刷新

## 技术细节

### 已修复的问题
1. ✅ Authorization 头缺失 → 所有 hooks 现在使用配置好的 axios 实例
2. ✅ Enum 序列化错误 → 所有 enum 使用 `.value` 而不是 `.name`
3. ✅ Mock 用户数据 → 创建了真实的用户列表 API
4. ✅ 重复格式化数据 → 路由直接使用 service 返回的数据
5. ✅ Redis 缓存错误数据 → 已清空缓存

### 当前系统配置
- **后端**: FastAPI + PostgreSQL + Redis + MinIO
- **前端**: React + TypeScript + Ant Design
- **认证**: 钉钉 OAuth + JWT
- **AI**: DeepSeek API (deepseek-v4-flash)
- **部署**: Docker Compose
- **访问**: ngrok 隧道

### 数据库状态
- 用户: 9 个（1 个真实 + 8 个测试）
- 合同: 1 个
- 评审: 1 个

## 下一步测试

完成基本功能测试后，可以测试：
1. 评审功能（提交评审意见）
2. 评论功能（添加评论和回复）
3. 附件上传（上传合同文件）
4. AI 总结（生成合同摘要）
5. 实时通知（WebSocket）

## 联系支持

如果问题仍然存在，请提供：
1. 浏览器控制台截图
2. 网络请求详情
3. 后端日志（最后 100 行）
4. 具体的操作步骤
